# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: PR Validation

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [ main, develop ]

permissions:
  contents: read
  issues: write
  pull-requests: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: windows-latest
    name: Validate Pull Request

    steps:
    - name: Checkout code
      uses: actions/checkout@v5
      with:
        submodules: false
        fetch-depth: 0

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Cache .NET packages
      uses: actions/cache@v4
      with:
        path: ~/.nuget/packages
        key: ${{ runner.os }}-nuget-${{ hashFiles('**/packages.lock.json') }}
        restore-keys: |
          ${{ runner.os }}-nuget-

    - name: Install frontend dependencies
      working-directory: frontend
      run: npm ci

    - name: Determine frontend lint targets
      id: frontend-lint-targets
      shell: bash
      run: |
        set -euo pipefail
        git fetch --no-tags --depth=1 origin "${{ github.base_ref }}"
        mapfile -t files < <(git diff --name-only --diff-filter=ACMR "origin/${{ github.base_ref }}...HEAD" -- 'frontend/*.js' 'frontend/*.jsx' 'frontend/*.ts' 'frontend/*.tsx' 'frontend/src/**/*.js' 'frontend/src/**/*.jsx' 'frontend/src/**/*.ts' 'frontend/src/**/*.tsx')
        if [ "${#files[@]}" -eq 0 ]; then
          echo "has_targets=false" >> "$GITHUB_OUTPUT"
          exit 0
        fi
        printf 'targets<<EOF\n' >> "$GITHUB_OUTPUT"
        printf '%s\n' "${files[@]#frontend/}" >> "$GITHUB_OUTPUT"
        printf 'EOF\n' >> "$GITHUB_OUTPUT"
        echo "has_targets=true" >> "$GITHUB_OUTPUT"

    - name: Run frontend linting (changed files)
      if: steps.frontend-lint-targets.outputs.has_targets == 'true'
      working-directory: frontend
      shell: bash
      run: |
        set -euo pipefail
        mapfile -t files <<'EOF'
        ${{ steps.frontend-lint-targets.outputs.targets }}
        EOF
        if [ "${#files[@]}" -gt 0 ]; then
          npx eslint "${files[@]}"
        fi

    - name: Skip frontend linting
      if: steps.frontend-lint-targets.outputs.has_targets != 'true'
      run: echo "No changed frontend source files to lint."


    - name: Check frontend build
      working-directory: frontend
      run: npm run build

    - name: Detect backend project
      id: backend-project
      shell: pwsh
      run: |
        $project = Get-ChildItem -Recurse -Filter *.csproj |
          Where-Object { $_.FullName -notmatch '\\node_modules\\' } |
          Select-Object -First 1 -ExpandProperty FullName
        if ($project) {
          "exists=true" >> $env:GITHUB_OUTPUT
          "path=$project" >> $env:GITHUB_OUTPUT
          Write-Host "Using .NET project: $project"
        } else {
          "exists=false" >> $env:GITHUB_OUTPUT
          Write-Host "No .NET project found; skipping .NET steps."
        }

    - name: Restore .NET dependencies
      if: steps.backend-project.outputs.exists == 'true'
      run: dotnet restore "${{ steps.backend-project.outputs.path }}"

    - name: Check backend build
      if: steps.backend-project.outputs.exists == 'true'
      run: dotnet build "${{ steps.backend-project.outputs.path }}" --configuration Release --no-restore

    - name: Run development setup (quick validation)
      if: hashFiles('scripts/dev-setup.ps1') != ''
      run: |
        .\scripts\dev-setup.ps1
      shell: powershell

    - name: Validate build scripts
      if: hashFiles('scripts/build-app.ps1', 'scripts/full-setup.ps1') != ''
      run: |
        # Test that build script can be parsed (syntax check)
        try {
          if (Test-Path .\scripts\build-app.ps1) {
            $null = [ScriptBlock]::Create((Get-Content .\scripts\build-app.ps1 -Raw))
          }
          if (Test-Path .\scripts\full-setup.ps1) {
            $null = [ScriptBlock]::Create((Get-Content .\scripts\full-setup.ps1 -Raw))
          }
          Write-Host "✓ Build scripts syntax validation passed" -ForegroundColor Green
        } catch {
          Write-Error "Build script syntax error: $_"
          exit 1
        }
      shell: powershell

    - name: Check for common issues
      run: |
        # Check for potential issues in the codebase
        $issues = @()
        
        # Check for hardcoded paths
        $hardcodedPaths = @()
        if (Test-Path "backend") {
          $hardcodedPaths = Select-String -Path "backend\*.cs" -Pattern "C:\\|D:\\|/Users/" -ErrorAction SilentlyContinue
        }
        if ($hardcodedPaths.Count -gt 0) {
          $issues += "Found potential hardcoded paths in backend code"
        }
        
        # Check for missing files that the build depends on
        $requiredFiles = @(
          "frontend\package.json",
          "scripts\dev-run.ps1"
        )
        
        foreach ($file in $requiredFiles) {
          if (-not (Test-Path $file)) {
            $issues += "Missing required file: $file"
          }
        }
        
        if ($issues.Count -gt 0) {
          Write-Warning "Found issues:"
          $issues | ForEach-Object { Write-Warning "- $_" }
          exit 1
        } else {
          Write-Host "No common issues found" -ForegroundColor Green
        }
      shell: powershell

    - name: Comment on PR
      if: github.event_name == 'pull_request'
      uses: actions/github-script@v7
      with:
        script: |
          const { data: comments } = await github.rest.issues.listComments({
            owner: context.repo.owner,
            repo: context.repo.repo,
            issue_number: context.issue.number,
          });
          
          const botComment = comments.find(comment => 
            comment.user.type === 'Bot' && 
            comment.body.includes('## PR Validation Results')
          );
          
          const body = `## PR Validation Results ✅
          
          The pull request has been validated successfully:
          
          - ✅ Frontend linting passed
          - ✅ Frontend build passed  
          - ✅ Backend build passed
          - ✅ Development setup validated
          - ✅ Build scripts syntax validated
          - ✅ No common issues detected
          
          The changes are ready for review!
          
          ---
          
          *This comment was automatically generated by the PR validation workflow.*`;
          
          if (botComment) {
            await github.rest.issues.updateComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: botComment.id,
              body: body
            });
          } else {
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: body
            });
          }
