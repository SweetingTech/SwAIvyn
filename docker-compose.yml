version: '3.8'

services:
  weaviate:
    image: semitechnologies/weaviate:1.27.0
    container_name: weaviate
    command:
      - --host
      - '0.0.0.0'
      - --port
      - '8080'
      - --scheme
      - 'http'
    environment:
      QUERY_DEFAULTS_LIMIT: '25'
      AUTHENTICATION_ANONYMOUS_ACCESS_ENABLED: 'true'
      PERSISTENCE_DATA_PATH: '/var/lib/weaviate'
      DEFAULT_VECTORIZER_MODULE: 'multi2vec-clip'
      ENABLE_MODULES: 'multi2vec-clip,qna-transformers,sum-transformers,text-spellcheck,reranker-transformers,backup-filesystem'
      CLUSTER_HOSTNAME: 'node1'
      BACKUP_FILESYSTEM_PATH: '/var/lib/weaviate/backups'
      CLIP_INFERENCE_API: 'http://multi2vec-clip:8080'
      QNA_INFERENCE_API: 'http://qna-transformers:8080'
      SUM_INFERENCE_API: 'http://sum-transformers:8080'
      SPELLCHECK_INFERENCE_API: 'http://text-spellcheck:8080'
      RERANKER_INFERENCE_API: 'http://reranker-transformers:8080'
    ports:
      - '8080:8080'
    volumes:
      - './weaviate_data:/var/lib/weaviate'
    restart: on-failure:0
    depends_on:
      - multi2vec-clip
      - qna-transformers
      - sum-transformers
      - text-spellcheck
      - reranker-transformers

  multi2vec-clip:
    image: semitechnologies/multi2vec-clip:sentence-transformers-clip-ViT-B-32-multilingual-v1
    container_name: multi2vec-clip
    environment:
      ENABLE_CUDA: '0'

  qna-transformers:
    image: semitechnologies/qna-transformers:distilbert-base-uncased-distilled-squad
    container_name: qna-transformers
    environment:
      ENABLE_CUDA: '0'

  sum-transformers:
    image: semitechnologies/sum-transformers:facebook-bart-large-cnn-1.0.0
    container_name: sum-transformers
    environment:
      ENABLE_CUDA: '0'

  text-spellcheck:
    image: semitechnologies/text-spellcheck-model:pyspellchecker-en
    container_name: text-spellcheck

  reranker-transformers:
    image: semitechnologies/reranker-transformers:cross-encoder-ms-marco-MiniLM-L-6-v2
    container_name: reranker-transformers
    environment:
      ENABLE_CUDA: '0'

  swai-tts:
    build:
      context: ./speech/TTS/openaudio-s1-mini
    ports:
      - "5002:5002"
    restart: unless-stopped


  frontend:
    build:
      context: ./frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
